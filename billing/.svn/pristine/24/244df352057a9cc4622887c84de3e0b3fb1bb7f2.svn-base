package com.tydic.beijing.billing.rating.domain;

import java.util.HashMap;
import java.util.Map;

public class PricingSectionRuleTree {

	private Map<Long,PricingSectionRuleNode> nodes=new HashMap<Long,PricingSectionRuleNode>();
	private Map<Long,PricingSectionRuleNode> rootNodes=new HashMap<Long,PricingSectionRuleNode>();
	private Map<Long,PricingSectionRuleNode> nodeRels=new HashMap<Long,PricingSectionRuleNode>();
	public Map<Long, PricingSectionRuleNode> getNodes() {
		return nodes;
	}
	public void setNodes(Map<Long, PricingSectionRuleNode> nodes) {
		this.nodes = nodes;
	}
	public Map<Long, PricingSectionRuleNode> getRootNodes() {
		return rootNodes;
	}
	public void setRootNodes(Map<Long, PricingSectionRuleNode> rootNodes) {
		this.rootNodes = rootNodes;
	}
	public Map<Long, PricingSectionRuleNode> getNodeRels() {
		return nodeRels;
	}
	public void setNodeRels(Map<Long, PricingSectionRuleNode> nodeRels) {
		this.nodeRels = nodeRels;
	}
	
	
	
}
